"""
智能造数（DataMake）领域子系统。

这个包承载你的《需求设计 v1》在 xagent 中的第一阶段代码骨架，
目标不是一次把所有能力做完，而是先把“谁负责什么、谁不负责什么”
这件事固化到目录和类结构里。

当前包的设计原则：
- 顶层由 `DataMakeReActPattern`（造数 ReAct 主控模式）负责业务主脑决策。
- 中间由 `application`（应用编排层）负责把决策分发到用户、审批、执行通道。
- 下层由 `guard`（护栏路由层）、`runtime`（运行执行层）、
  `resources`（资源适配层）、`ledger`（账本留痕层）协同完成动作落地。
- `contracts`（领域契约层）负责放跨层共享的结构定义。
- `services`（领域辅助服务层）负责 recall、draft、approval 等辅助能力。

注意：
- 当前阶段只搭建清晰分层和职责注释，不补完整核心数据模型。
- 注释会显式采用“英文术语（中文解释）”写法，方便后续团队统一名词。
"""
